apiVersion: v1
kind: Service
metadata:
  name: oficina-api-service
  namespace: oficina-api
  labels:
    app: oficina-api
spec:
  selector:
    app: oficina-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
